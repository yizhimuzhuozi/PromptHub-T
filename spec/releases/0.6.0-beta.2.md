# PromptHub 0.6.0-beta.2 Release Record

## Release

- Version: `0.6.0-beta.2`
- Tag: `v0.6.0-beta.2`
- Candidate commit: `b7854508c3c69eb99cc2d1b6207db02ce5a25ddc`
- Date: 2026-09-03
- Channel: Preview / beta
- Status: Published prerelease at 2026-09-03 09:52:55 UTC
- Release: https://github.com/legeling/PromptHub/releases/tag/v0.6.0-beta.2
- Published stable remains: `v0.5.9`
- Previous preview: `v0.6.0-beta.1`

## Summary

`0.6.0-beta.2` is the second preview of the 0.6 line. It adds whole-image
editing, Doubao Work Skill support, and repository Playwright Test Agents, while
hardening Git-less public Skill installation, Rules restore, upgrade recovery,
managed backup retention, updater sources, remembered close behavior, OpenCode
session discovery, and several Agent/Plugin/MCP persistence boundaries.

## Release Surfaces

- Root, Desktop, CLI, self-hosted Web, Cloudflare Worker, Mobile package, Expo
  metadata, and the standalone CLI runtime use `0.6.0-beta.2`.
- Private shared packages retain their independent `0.1.0` versions.
- Localized README preview rows and summaries target `v0.6.0-beta.2`.
- Stable badges, default downloads, website generated release metadata,
  Homebrew guidance, and GHCR `latest` remain on `v0.5.9`.
- Existing public screenshots remain unchanged. This preparation updates
  release metadata and notes; it does not claim a new public screenshot set.

## Required Verification

- Local `pnpm verify:release:quick` must pass before the candidate is frozen.
- Full `pnpm verify:release` must pass before tag creation.
- The tag-triggered workflow must pass Linux and Windows packaging, the Windows
  x64 two-launch upgrade gate, signed/notarized macOS x64 and arm64 builds,
  updater-manifest and asset verification, and prerelease-only GHCR tagging.
- Public representative asset URLs and the GitHub prerelease state must be
  checked after publication.

## Local Preparation Evidence

- `pnpm verify:release:quick`: passed all 29 checks.
- `pnpm verify:release`: passed all 42 checks in 704.8 seconds, including
  builds, performance budgets, unit/integration suites, and seven
  built-artifact Electron E2E tests.
- Release metadata, CLI runtime, localized preview links, stable website
  metadata, specification traceability, and formatting/diff hygiene checks
  passed.

## Publication Evidence

- Desktop Build and Release run `33738828038` passed the full 42-check release
  profile, Linux packaging, Windows x64 and arm64 packaging, signed/notarized
  macOS x64 and arm64 packaging, and the release job. Windows x64 passed the
  packaged two-launch startup smoke.
- Release job `100602404548` verified asset hashes and uploaded 20 assets. The
  GitHub asset API reports all 20 as uploaded. Representative SHA-256 digests:
  - Windows x64 installer:
    `4d239ddfefb7dfa2abcbcc19d997af650f6931c9f1273c6c7dc334cfbcb23513`
  - macOS arm64 DMG:
    `85600071c98179c350aa5d788831b46db7621304d5c428a9d87e7aad9e319c5c`
  - Linux x64 AppImage:
    `d5bf5bdea8a3b0722a41ea2d305805a6a33b5e94ae4a5ddf8cf51bddbfffb448`
  - CLI tarball:
    `9faa9fde515d7d0721cb79cc32bcf6ebda986a6f8d227347e80592f3c2471205`
  - Windows x64 update manifest:
    `85092d8b0fb47b5e5ba539c54b5bf9f9499dcbbe6666d6cde678f8962f3cde39`
- Self-Hosted Web run `33738827888` passed verification and published GHCR
  digest
  `sha256:677e4d9e6239e53d0a704490bf5b48727ffbe58caa0ec56ee464b9d669e73068`
  under the beta.2 tag.
- Independent checks returned HTTP 200 for the release page, `latest.yml`, the
  Windows x64 installer, and the macOS arm64 DMG. GitHub reports the release as
  non-draft and prerelease with all 20 assets uploaded.
- GitHub still reports `v0.5.9` as Latest. GHCR `latest` and `0.5.9` both remain
  on stable digest
  `sha256:110d9c320236a7bc41fc624b564db58b1f71c13a110ee02b198e0024abc3784e`.

## Known Boundaries

- Issue #211 was closed by the reporter after installing Git, and the fallback
  has live GitHub evidence, but the Git-less path still lacks packaged Windows
  UI acceptance.
- Whole-image editing has deterministic transport and isolated Electron
  evidence, but no paid Provider request was made.
- Desktop settings authority convergence remains a planned follow-up; the
  remembered-close fix uses the current compatibility bridge.

## Publication Contract

- The tag-triggered workflow created a Draft release first. Publication occurred
  only after the full platform, signing/notarization, packaged-startup, asset,
  updater-manifest, and container checks passed.
- The published release is a Prerelease, never Latest. The beta.1 tag was not
  moved or replaced.
- Stable `0.5.9` downloads, Homebrew metadata, website defaults, and GHCR
  aliases remain unchanged.
