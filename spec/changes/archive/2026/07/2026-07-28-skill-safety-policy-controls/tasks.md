# Tasks

- [x] `T-SAFETY-001` Add failing resolver and settings migration tests for
      `FR-SAFETY-001`, `FR-SAFETY-002`, `NFR-SAFETY-002`;
      implement `DES-SAFETY-001` and `DES-SAFETY-002`.
- [x] `T-SAFETY-002` Add failing controller/component tests proving disabled
      install/update paths do not invoke automatic scans for
      `FR-SAFETY-003`; thread exact source policy through Store operations.
- [x] `T-SAFETY-003` Add settings component tests and implement the localized
      global/channel/store controls for `FR-SAFETY-001`,
      `FR-SAFETY-002`, and `FR-SAFETY-004`.
- [x] `T-SAFETY-004` Add shared/core/main contract tests for explicit scan mode
      and immutable validation; implement `DES-SAFETY-003` for
      `FR-SAFETY-003` and `NFR-SAFETY-001`.
- [x] `T-SAFETY-005` Run focused tests, type checks, and a bounded settings-list
      performance assertion; record actual results.
- [x] `T-SAFETY-006` Update stable Skill Store requirements, regression matrix,
      user-facing safety documentation, and this change implementation record;
      complete converge and archive the change.
