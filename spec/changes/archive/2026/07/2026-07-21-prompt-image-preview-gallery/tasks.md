# Prompt Image Preview Gallery Tasks

- [x] `T-PIP-001` Add failing `TEST-PIP-001` component regressions for clicked-image
      position, bounded buttons, keyboard navigation, single-image fallback and error
      recovery. Covers `FR-PIP-001` and `DES-PIP-001`.
- [x] `T-PIP-002` Implement the shared modal gallery UI, pass Prompt image order from both
      detail surfaces, and add seven-locale accessible labels. Covers `NFR-PIP-001`,
      `DES-PIP-002` and `TEST-PIP-002`.
- [x] `T-PIP-003` Run targeted component/i18n tests, typecheck, ESLint, Prettier, spec
      governance, diff validation and Desktop build; record actual outcomes in
      `implementation.md`.
- [x] `T-PIP-004` Converge the stable Desktop behavior document and archive the change only
      after the implementation is ready for delivery.
