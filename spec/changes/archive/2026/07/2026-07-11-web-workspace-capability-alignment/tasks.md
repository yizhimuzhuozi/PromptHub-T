# Tasks

- [x] `T-WEB-CORE-001` Add failing route tests for hierarchy, relations, and
  output formats, then implement validated durable APIs (`FR-WEB-CORE-001`,
  `NFR-WEB-CORE-001`, `DES-WEB-CORE-001/002`).
- [x] `T-WEB-CORE-002` Map the Web bridge to the new Prompt APIs and prove it
  cannot use browser-local persistence (`FR-WEB-CORE-001`,
  `DES-WEB-CORE-003`).
- [x] `T-WEB-CORE-003` Add Web capability filtering and remove unsupported
  browser modules/actions from the shared renderer (`FR-WEB-CORE-002`,
  `DES-WEB-CORE-004/005`).
- [x] `T-WEB-CORE-004` Verify agent asset snapshots remain preserved by sync
  and backup contracts (`FR-WEB-CORE-003`, `DES-WEB-CORE-005`).
- [x] `T-WEB-CORE-005` Run focused coverage, browser workflow checks, the Web
    verifier, release harness, and documentation convergence. Focused tests,
    Web lint/typecheck/build, browser verification, and documentation converge
    are complete; the final 22-stage release harness passed on 2026-07-11.
