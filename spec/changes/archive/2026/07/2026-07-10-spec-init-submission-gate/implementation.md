# Implementation

## Status

Implemented.

## Shipped

- Added localized `docs/rules/commit-rules.md` scaffold templates for Chinese
  and English projects.
- Wired `scripts/spec-init.sh` to generate commit rules and registered the file
  in both localized rules indexes and AGENTS templates.
- Added a scaffold regression test that initializes both supported languages
  and verifies the generated rule, AGENTS link, and mandatory test-status copy.
- Added an explicit PromptHub submission gate to the embedded skill, root
  `AGENTS.md`, and `spec/rules/submission-traceability-rules.md`.
- Required every non-trivial commit to include a body with its primary
  change/issue reference and actual verification status.

## Verification

- Red test before implementation:
  `bash .agents/skills/spec-init/tests/spec-init-commit-rules.sh`
  - Result: failed because the scaffold did not generate
    `docs/rules/commit-rules.md`.
- `bash .agents/skills/spec-init/tests/spec-init-commit-rules.sh`
  - Result: passed for Chinese and English scaffolds.
- `bash -n .agents/skills/spec-init/scripts/spec-init.sh .agents/skills/spec-init/tests/spec-init-commit-rules.sh`
  - Result: passed.
- `python3 /Users/lingxiaotian/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/spec-init`
  - Result: passed (`Skill is valid!`).
- `git diff --check -- .agents/skills/spec-init AGENTS.md spec/rules/submission-traceability-rules.md spec/changes/active/spec-init-submission-gate`
  - Result: passed.
- `pnpm exec prettier --check --parser markdown <touched .tmpl and example AGENTS files>`
  - Result: passed after formatting the touched templates.
- `pnpm exec prettier --check <touched skill, rule, example, and change Markdown files>`
  - Result: passed.

## Residual Risk

- Existing projects generated by older `spec-init` versions require an
  explicit rules refresh; the scaffold does not overwrite them automatically.
