# Agent Rules Source Matrix Tasks

- [x] `T-RULESRC-001` Establish the design, issue routing, source-of-truth, and
      relationship to #193.
- [ ] `T-RULESRC-002` Research official current platform contracts and create a
      versioned compatibility fixture before enabling any registry row.
- [ ] `T-RULESRC-003` Add failing Claude Windows/macOS/Linux user/project
      directory tests, then implement the directory adapter.
- [ ] `T-RULESRC-004` Add managed metadata migration, batch reconciliation,
      preview/edit/version behavior, and source badges.
- [ ] `T-RULESRC-005` Add confirmed adapters one platform at a time with
      false-positive and precedence tests.
- [ ] `T-RULESRC-006` Run traversal fuzz/security, large-tree stress, write-count,
      coverage, typecheck, and real desktop verification.
- [ ] `T-RULESRC-007` Sync the stable Rules boundary and compatibility matrix;
      converge #196/#197 and archive only after release evidence.
- [x] `T-RULESRC-008` Add test-first Kiro, Augment, and Cline global entry
      descriptors, enable their Agent Rules capability through the shared
      registry, verify explicit creation and exact resolved paths, and document
      the richer sibling/project rule surfaces that remain out of scope.
