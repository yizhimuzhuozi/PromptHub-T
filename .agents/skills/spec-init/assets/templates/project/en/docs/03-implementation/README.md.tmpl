# Implementation Plan: __PROJECT_NAME__

> Updated on: __DATE__
>
> This document arranges delivery order and does not replace the design document.

## 1. Delivery Goal

[What scope this phase is meant to deliver]

## 2. Milestones

| Milestone | Goal | Input | Output | Acceptance |
|---|---|---|---|---|
| `M1` | [Foundation] | `docs/workflow/01-requirements/README.md`, `docs/workflow/02-design/README.md` | [code / config / tests] | [how to verify] |
| `M2` | [Core feature] | `M1` | [code / config / tests] | [how to verify] |

## 3. Suggested Order

1. [What to do first]
2. [What to do next]
3. [What to do last]

## 3.1 Phased Scope Guidance

- Let V1 deliver one primary end-to-end chain that can already be validated.
- Move multi-role branches, batch actions, advanced reporting, and open integrations to V2 unless they are essential.
- If you are unsure whether something belongs in V1, ask: without it, can V1 still prove the product's value? If yes, delay it.

Notes:

- Phasing does not mean planning only once. After each delivery stage, come back and refine requirements, design, verification, and tasks.

## 3.2 Ongoing Refinement Plan

- Which completed changes should update `docs/workflow/01-requirements/README.md`
- Which implemented decisions should update `docs/workflow/02-design/README.md`
- Which long-lived truths should be captured under `docs/knowledge/`
- Which new tests should update `docs/workflow/04-verification/README.md`
- Which completed tasks reveal new follow-up work that should be written into `docs/workflow/05-tasks/README.md`

## 4. Dependencies

| Item | Dependency | Why |
|---|---|---|
| [Capability / module] | [Prerequisite] | [Reason] |

## 5. High-Risk First Items

- [Validate the most failure-prone or expensive part first]
- [Move technical uncertainty earlier when possible]

## 6. Documentation and Testing Sync Plan

- Which implementation stages must update `docs/workflow/02-design/README.md`
- Which stages must update `docs/workflow/04-verification/README.md` first
- Which changes impact `README.md`, `AGENTS.md`, or `docs/rules/`

## 7. Release / Rollback Strategy

- Release mode: [big bang / staged / gradual]
- Rollback plan: [how to revert if it fails]

## 8. Traceability Delivery Plan

Explain which traceability chains are delivered in each milestone:

- `M1`: Which `FR -> DES -> TEST -> T` chain is delivered first
- `M2`: Which requirement chains are added next
- If some requirements move to a later version, note that here explicitly

## 9. Analysis and Convergence Plan

- Pre-implementation analyze: check whether `FR-* / DES-* / TEST-* / T-*` are fully connected.
- Pre-implementation analyze: check for blocking `[To confirm]` items, conflicting docs, or floating tasks.
- Post-implementation converge: write real code behavior, test results, and newly discovered long-lived truth back into workflow and knowledge docs.
- Post-implementation converge: update the status of `docs/changes/active/<change-key>/`, moving it to `completed/` or `legacy/` when appropriate.
- Post-implementation converge: sync affected README, AGENTS, issues, releases, ADRs, and archive entries.

## 10. Implementation Plan Checklist

- This is a delivery order document, not a second design document
- High-risk and high-uncertainty items are validated early
- Every milestone has observable acceptance output
- Doc updates and test updates are part of the plan
- Obvious enhancement-only items are not forced into V1
- analyze / converge are part of the delivery rhythm, not afterthoughts

## 11. Blockers and Open Questions

- [To confirm]
- [To confirm]
