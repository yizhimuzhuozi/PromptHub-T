interface:
  display_name: "spec-init"
  short_description: "Run a Spec Kit-inspired docs workflow with workflow, knowledge, changes, analysis, convergence, README, and AGENTS."
  brand_color: "#0F766E"
  default_prompt: "Use $spec-init to specify, clarify, plan, task, analyze, implement, and converge project docs around workflow, knowledge, changes, README, and AGENTS."

policy:
  allow_implicit_invocation: true
